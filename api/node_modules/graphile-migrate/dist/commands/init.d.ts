import { CommandModule } from "yargs";
interface InitOptions {
    folder?: boolean;
}
export declare function init(options?: InitOptions): Promise<void>;
export declare const initCommand: CommandModule<{}, InitOptions>;
export {};
